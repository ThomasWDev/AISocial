//
//  RapidAPIView.swift
//  ChatAI2
//
//  Created by Thomas Woodfinon 4/4/23.
//

/*
import SwiftUI

struct RapidAPIView: View {
    @StateObject private var viewModel = RapidAPIViewModel()

    var body: some View {
        ScrollView {
            Text("Getting Response from RapidAPI then ChatGPT..")
                .bold()
                .padding(16)
            if let response = viewModel.chatGPTResponse {
                Text(response)
                    .padding(16)
            }
        }
        .showLoadingView(viewModel.isLoading)
        .onAppear {
            viewModel.onAppear()
        }
    }
}

struct RapidAPIView_Previews: PreviewProvider {
    static var previews: some View {
        RapidAPIView()
    }
}
*/
